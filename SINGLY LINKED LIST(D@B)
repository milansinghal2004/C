// deletion from beginning
void bdel()
{
    struct node *temp = head;
    if(head == NULL)
    {
        printf("Underflow");
    }   
    else
    {
        head = head -> next;
        free(temp);
        temp = NULL;
    }
}
