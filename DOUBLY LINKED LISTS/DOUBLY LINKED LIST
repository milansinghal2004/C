// traversal
void trav()
{
    struct node *temp = head;
    if(head == NULL)
    {
        printf("No list exist");
    }
    else
    {
        while(temp != NULL)
        {
            printf("%d\t", temp -> data);
            temp = temp -> next;
        }
    }
}

