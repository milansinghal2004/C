// PUSHING ELEMENT
void push(int n)
{
    int x;
    if (top == max - 1)
    {
        printf("Overflow!!");
    }
    else
    {
        printf("Enter the data : ");
        scanf("%d", &x);
        top = top + 1;
        stack[top] = x;
    }
}
 
